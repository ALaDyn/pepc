# @ job_name = coulomb
# @ comment = "BGL Job by Size"
# @ error = $(job_name).$(jobid).out
# @ output = $(job_name).$(jobid).out
# @ environment = COPY_ALL;
# @ bg_connection = MESH 
# @ wall_clock_limit = 00:30:00
# @ notification = complete
# @ notify_user = p.gibbon@fz-juelich.de
# @ job_type = bluegene
# @ bg_size = 128 
# @ queue
PEPC=../bin/pepcb
cp coulomb_sa.h run.h
/usr/local/bin/mpirun -exe `/bin/pwd`/$PEPC -mode VN -np 250 -verbose 1 -args "-t 1"
