# -- required defaults
#@ job_name = md
#@ job_type = parallel 
#@ error = $(job_name).$(jobid).out
#@ output = $(job_name).$(jobid).out
#@ notification = complete
#@ notify_user = r.speck@fz-juelich.de
#@ wall_clock_limit = 00:30:00
#@ node = 1
#@ tasks_per_node = 4
#@ data_limit=3.0GB 
#@ stack_limit=0.5GB
#@ node_usage = shared
#@ queue 


PEPC=../bin/pepce_power6
cp eqm.h run.h
poe $PEPC
