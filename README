To compile this program please define makefile.defs in the root directory. When you are at Jugene and have entered the root directory, the command can look like this:

ln -sf ./makefiles/makefile.defs.jugene ./makefile.defs

Some machines defs are already predefined and can be found in makefiles/
