# @ job_name = cyl
# @ comment = "PEPC capacitor plate config"
# @ error = $(job_name).$(jobid).out
# @ output = $(job_name).$(jobid).out
# @ environment = COPY_ALL;
# @ bg_connectivity = MESH 
# @ wall_clock_limit = 00:30:00
# @ notification = error
# @ notify_user = p.gibbon@fz-juelich.de
# @ job_type = bluegene
# @ bg_size =32	 
# @ queue
#mkdir $WORK/bench/$(job_name)
runjob --np 32 --ranks-per-node 1 --exe ../../bin-bgq/pepc-b --args "./plates.h"
