        COMMON /VTdefsC/ ICLASSH, &
        VTNOSCL, &
       IF_beam,&
	IF_beam_control,&
	IF_blank6,&
	IF_blankn,&
	IF_check_table,&
	IF_closefiles,&
	IF_cold_start,&
	IF_configure,&
	IF_constrain,&
	IF_cput,&
	IF_diagnose_tree,&
	IF_diagnostics,&
	IF_draw2d,&
	IF_draw2d_hash,&
	IF_draw_domains,&
	IF_draw_lists,&
	IF_draw_map,&
	IF_draw_tree2d,&
	IF_driver,&
	IF_dump,&
	IF_energy_cons,&
	IF_forces,&
	IF_forces_bal,&
	IF_fpond,&
	IF_genran,&
	IF_indsort_i,&
	IF_key2addr,&
	IF_key2node,&
	IF_keytest,&
	IF_kinenergy,&
	IF_make_branches,&
	IF_make_domains,&
	IF_make_hashentry,&
	IF_maxwell1,&
	IF_mc_config,&
	IF_nwaymrg,&
	IF_openfiles,&
	IF_phase,&
	IF_potenergy,&
	IF_predef_parts,&
	IF_psrsperm_i4,&
	IF_psrsperm_i8,&
	IF_psrsperm_r8,&
	IF_psrssort,&
	IF_pswssort,&
	IF_push,&
	IF_randion,&
	IF_rano,&
	IF_rantest,&
	IF_reset_ions,&
	IF_scramble_v,&
	IF_setup,&
	IF_sift_down,&
	IF_sort_i,&
	IF_sum_force,&
	IF_sumpot,&
	IF_swap_ab,&
	IF_times2,&
	IF_track_nc,&
	IF_tree_build,&
	IF_tree_fill,&
	IF_tree_properties,&
	IF_tree_walk,&
	IF_treemp,&
	IF_velocities,&
	IF_visit_dump,&
	IF_visualize
        INTEGER*4 IF_beam
        INTEGER*4 IF_beam_control
        INTEGER*4 IF_blank6
        INTEGER*4 IF_blankn
        INTEGER*4 IF_check_table
        INTEGER*4 IF_closefiles
        INTEGER*4 IF_cold_start
        INTEGER*4 IF_configure
        INTEGER*4 IF_constrain
        INTEGER*4 IF_cput
        INTEGER*4 IF_diagnose_tree
        INTEGER*4 IF_diagnostics
        INTEGER*4 IF_draw2d
        INTEGER*4 IF_draw2d_hash
        INTEGER*4 IF_draw_domains
        INTEGER*4 IF_draw_lists
        INTEGER*4 IF_draw_map
        INTEGER*4 IF_draw_tree2d
        INTEGER*4 IF_driver
        INTEGER*4 IF_dump
        INTEGER*4 IF_energy_cons
        INTEGER*4 IF_forces
        INTEGER*4 IF_forces_bal
        INTEGER*4 IF_fpond
        INTEGER*4 IF_genran
        INTEGER*4 IF_indsort_i
        INTEGER*4 IF_key2addr
        INTEGER*4 IF_key2node
        INTEGER*4 IF_keytest
        INTEGER*4 IF_kinenergy
        INTEGER*4 IF_make_branches
        INTEGER*4 IF_make_domains
        INTEGER*4 IF_make_hashentry
        INTEGER*4 IF_maxwell1
        INTEGER*4 IF_mc_config
        INTEGER*4 IF_nwaymrg
        INTEGER*4 IF_openfiles
        INTEGER*4 IF_phase
        INTEGER*4 IF_potenergy
        INTEGER*4 IF_predef_parts
        INTEGER*4 IF_psrsperm_i4
        INTEGER*4 IF_psrsperm_i8
        INTEGER*4 IF_psrsperm_r8
        INTEGER*4 IF_psrssort
        INTEGER*4 IF_pswssort
        INTEGER*4 IF_push
        INTEGER*4 IF_randion
        INTEGER*4 IF_rano
        INTEGER*4 IF_rantest
        INTEGER*4 IF_reset_ions
        INTEGER*4 IF_scramble_v
        INTEGER*4 IF_setup
        INTEGER*4 IF_sift_down
        INTEGER*4 IF_sort_i
        INTEGER*4 IF_sum_force
        INTEGER*4 IF_sumpot
        INTEGER*4 IF_swap_ab
        INTEGER*4 IF_times2
        INTEGER*4 IF_track_nc
        INTEGER*4 IF_tree_build
        INTEGER*4 IF_tree_fill
        INTEGER*4 IF_tree_properties
        INTEGER*4 IF_tree_walk
        INTEGER*4 IF_treemp
        INTEGER*4 IF_velocities
        INTEGER*4 IF_visit_dump
        INTEGER*4 IF_visualize
        INTEGER*4 ICLASSH
        INTEGER*4 VTNOSCL
